[package]
name = "mini-gta"
version = "0.1.0"
edition = "2021"
authors = ["Mini GTA"]
description = "A browser-style mini-GTA, ported from Three.js to Bevy"
license = "MIT"

[dependencies]
bevy = "0.15"
bevy_egui = "0.31"
rand = "0.8"

[profile.dev]
opt-level = 2

# Always compile dependencies (Bevy & co) with full optimizations,
# even in debug builds — big runtime FPS win for `cargo run`.
[profile.dev.package."*"]
opt-level = 3

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
